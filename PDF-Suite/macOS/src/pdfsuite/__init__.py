"""
PT-PT: PDF Suite — duas ferramentas que partilham a mesma leitura de documentos.

       A primeira transforma um PDF estático num PDF preenchível, detectando
       sozinha onde ficam os campos e deixando o utilizador corrigi-los antes
       de gravar.

       A segunda lê vários documentos — PDF, Word, texto — e compara-os. O caso
       de uso que lhe deu origem são propostas de fornecedores: seis PDF de
       seis vendedores diferentes, e a pergunta «qual é a melhor». Também
       resume um documento único.

EN-UK: PDF Suite — two tools sharing the same document reading layer.

       The first turns a static PDF into a fillable one, working out where the
       fields go and letting the user correct them before saving.

       The second reads several documents — PDF, Word, text — and compares
       them. The use case behind it is vendor quotes: six PDFs from six
       different vendors, and the question "which is best". It also summarises
       a single document.

Created by Redfox using Claude
"""

__version__ = "2.0.0"
__app_name__ = "PDF Suite"
__author__ = "Rafael Santos (Redfox)"
__license__ = "MIT"
__credit__ = "Created by Redfox using Claude"

__all__ = ["__version__", "__app_name__", "__author__", "__license__", "__credit__"]
