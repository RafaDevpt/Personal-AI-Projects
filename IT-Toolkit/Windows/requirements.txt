# ===========================================================================
# PT-PT: Dependências de execução.
#        Instalar com:  pip install -r requirements.txt
#
#        São apenas duas, de propósito. Toda a recolha — event logs, rede,
#        discos, serviços, inventário e geração dos relatórios HTML — está
#        implementada com a biblioteca padrão e com as ferramentas que já vêm
#        no Windows, porque esta ferramenta corre em máquinas de domínio onde
#        instalar pacotes é lento ou está bloqueado por política.
#
# EN-UK: Runtime dependencies.
#        Install with:  pip install -r requirements.txt
#
#        There are only two, deliberately. All collection — event logs,
#        network, disks, services, inventory and HTML report generation — is
#        implemented with the standard library and the tools Windows already
#        ships, because this tool runs on domain machines where installing
#        packages is slow or blocked by policy.
# ===========================================================================

# PT-PT: Interface gráfica. Assenta no Tkinter da biblioteca padrão, pelo que
#        não traz nenhum motor gráfico próprio. Licença MIT.
#        Não é necessária no modo --cli.
# EN-UK: Graphical interface. Sits on the standard library's Tkinter, so it
#        brings no graphics engine of its own. MIT-licensed.
#        Not required in --cli mode.
customtkinter>=5.2.2,<6.0

# PT-PT: Leitura de processador, memória e partições. Se faltar, a aplicação
#        abre na mesma e esses valores ficam indisponíveis — ver o cabeçalho
#        de src/ittoolkit/system.py.
# EN-UK: Reading processor, memory and partitions. If missing, the application
#        still opens and those values become unavailable — see the header of
#        src/ittoolkit/system.py.
psutil>=5.9,<7.0

# ===========================================================================
# PT-PT: Em Linux pode faltar o Tkinter: sudo apt install python3-tk
# EN-UK: On Linux, Tkinter may be missing: sudo apt install python3-tk
# ===========================================================================
