# ===========================================================================
# PT-PT: Dependências de execução.
#        Instalar com:  pip install -r requirements.txt
#
#        São apenas duas, de propósito. Toda a lógica de rede — SNMP, BER,
#        HTTP, geração de PDF — está implementada com a biblioteca padrão,
#        porque esta ferramenta corre em máquinas de domínio onde instalar
#        pacotes é lento ou está bloqueado por política.
#
# EN-UK: Runtime dependencies.
#        Install with:  pip install -r requirements.txt
#
#        There are only two, deliberately. All the network logic — SNMP, BER,
#        HTTP, PDF generation — is implemented with the standard library,
#        because this tool runs on domain machines where installing packages is
#        slow or blocked by policy.
# ===========================================================================

# PT-PT: Leitura e escrita do inventário em Excel.
# EN-UK: Reading and writing the Excel inventory.
openpyxl>=3.1,<4.0

# PT-PT: Interface gráfica. Assenta no Tkinter da biblioteca padrão, pelo que
#        não traz nenhum motor gráfico próprio. Licença MIT.
#        Não é necessária no modo --cli.
# EN-UK: Graphical interface. Sits on the standard library's Tkinter, so it
#        brings no graphics engine of its own. MIT-licensed.
#        Not required in --cli mode.
customtkinter>=5.2.2,<6.0

# ===========================================================================
# PT-PT: Em Linux pode faltar o Tkinter: sudo apt install python3-tk
# EN-UK: On Linux, Tkinter may be missing: sudo apt install python3-tk
# ===========================================================================
