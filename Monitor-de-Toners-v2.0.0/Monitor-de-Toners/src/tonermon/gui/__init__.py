#!/usr/bin/env python3
"""
PT-PT: Camada de interface gráfica, construída sobre CustomTkinter.
       CustomTkinter assenta no Tkinter que já vem com o Python, tem licença
       MIT e produz executáveis pequenos com o PyInstaller — o que importa numa
       ferramenta distribuída por máquinas de domínio.

EN-UK: Graphical interface layer, built on CustomTkinter.
       CustomTkinter sits on the Tkinter that already ships with Python, is
       MIT-licensed and produces small PyInstaller builds — which matters for a
       tool deployed across domain machines.

Created by Redfox using Claude
"""
